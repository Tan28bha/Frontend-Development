let cars=['BMW','Audi','Tata']
console.log(cars[2]);

// 🪄 3. Adding and Removing Elements

let numbers = [1, 2, 3];
numbers.push(4);    // [1,2,3,4]
numbers.pop();      // [1,2,3]
numbers.unshift(0); // [0,1,2,3]
numbers.shift();    // [1,2,3]
